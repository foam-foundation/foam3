p({
  "class": "foam.core.boot.CSpec",
  "name": "compactionTestRecordDAO",
  "serve": true,
  "lazy": true,
  "authenticate": false,
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.dao.compaction.test.TestRecord.getOwnClassInfo())
      .setJournalName("compactionTestRecords")
      .build();
  """
})
